window.app = angular.module('mean', ['ngCookies', 'ngResource', 'ui.bootstrap', 'ui.router', 'mean.system']);

angular.module('mean.system', []);

// Servant Global Functions
app.run(function($rootScope, $state, $stateParams, $timeout, $interval, $window) {
	
	// Initialize ---------------------------

	// Defaults -----------------------------

	// Universal Functions ------------------


}); // app.run

console.log("hereere")